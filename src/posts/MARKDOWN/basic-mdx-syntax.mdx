---
title: Basic Mdx with remark-gfm Plugin Syntax
date: "2023/08/18"
description: basic markdown syntax that we can use with addition plugin by of remark-gfm through next-remote-mdx
tags: 'mdx,markdown,remark-gfm,next-mdx-remote' 
published: true
---

## Heading

Heading is commonly used to define something important or give attention to reader 
We can use it by adding hash(#), and everytime hash(#) increases smaller the size of text it is

```markdown
hash from 1 ~ 6
# 1 hash Biggest
## 2 hash
### 3 hash
#### 4 hash
##### 5 hash
###### 6 hash Smallest
####### 7 doesn't work
```
 
#### Heading Result

# 1 hash Biggest

## 2 hash

### 3 hash

#### 4 hash

##### 5 hash

###### 6 hash Smallest

## Ordered List

Ordered list is define by number with a dot

Even though we define list unorderly, it does not order by itself<br/>
please make sure that the number is ordered

```markdown
1. one
2. two
3. three

1. one
3. three
2. two
```

#### Ordered List Result

1. one
2. two
3. three

## Unordered List

Unordered List is define by -

```markdown
- parent
    - child
        - descendent
```

#### Unordered List Result

- parent
  - child
    - descendent

## Check List

Check List is define by -`[ ] for uncheck [x] for check`

```markdown
* [x] finish
* [ ] todo

```

#### Check List Result

* [x] finish
* [ ] todo

## Next Line

```markdown
<br/>

test<br/>test<br/><br/>test

```

#### Next Line Result

test<br/>test<br/><br/>test

## Code Block

Code Block usually opens by tripple grave \`\`\` and close by tripple grave \`\`\`

````js showLineNumbers 
```(open tripple grave)

export default function PostTags({ tags }: { tags: string }) {
  return (
    <>
      {tags.split(",").map((tag) => (
        <span
          key={tag}
          className="test"
        >
          {tag}
        </span>
      ))}
    </>
  );
}


(close tripple grave)```
````

#### Code Result
```tsx:sample.tsx js showLineNumbers 
export default function PostTags({ tags }: { tags: string }) {
  return (
    <>
      {tags.split(",").map((tag) => (
        <span
          key={tag}
          className="test"
        >
          {tag}
        </span>
      ))}
    </>
  );
}
```

## Delimeter

Delimeter is define by `<hr/>` `---`

```markdown
between any contents

---

<hr/>
```

#### Delimeter Result

contents

<hr />

---

## URL Link / Email Link

Link is define by `[name](link)`

```markdown
URL Link
[Google](https://google.com)

```

#### Link Result

Link: [Google](https://google.com)<br/>

## Text Type

Text Type of Strong, em, strike

```markdown

I am _Seogki Hong_ (emphasis)

I am **Seogki Hong** (strong)

I am ~Seogki Hong~ (strike)
I am ~~Seogki Hong~~ (strike)

```

#### Text Type Result

I am _Seogki Hong_ (emphasis)

I am **Seogki Hong** (strong)

I am ~Seogki Hong~ (strike)

I am ~~Seogki Hong~~ (strike)


## Image

Image is define by `![image name](/path/image.jpg|png)`

```markdown
default
![Sample](./image.jpg)

public path
![Sample](/posts/MARKDOWN/basic-mdx-syntax/sample-image.jpg)
```

#### Image Result

 

![Sample](/posts/MARKDOWN/basic-mdx-syntax/sample-image.jpg)

## Table

Table is define by combination of `|`, `:`, `-`

```markdown
| a | b  |  c |  d  |
| - | :- | -: | :-: | 
| 1 | 2  |  3 |  4  |
```

| a | b  |  c |  d  |
| - | :- | -: | :-: |
| 1 | 2  |  3 |  4  |



<hr />

## Reference 🔗

https://www.markdownguide.org/<br/>
https://gist.github.com/ihoneymon/652be052a0727ad59601<br/>
https://mdxjs.com/docs/what-is-mdx/#mdx-syntax<br/>
